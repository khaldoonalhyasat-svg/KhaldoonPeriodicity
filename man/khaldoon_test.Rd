% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/khaldoon_test.R
\name{khaldoon_test}
\alias{khaldoon_test}
\title{Khaldoon Periodicity Test}
\usage{
khaldoon_test(
  x,
  n_bootstrap = 1000,
  significance_threshold = 0.05,
  parallel = FALSE,
  n_cores = NULL
)
}
\arguments{
\item{x}{A numeric vector containing the time series data}

\item{n_bootstrap}{Number of bootstrap replications (default: 1000)}

\item{significance_threshold}{Significance level for the test (default: 0.05)}

\item{parallel}{Logical indicating whether to use parallel processing (default: FALSE)}

\item{n_cores}{Number of cores to use for parallel processing (default: NULL = auto-detect)}
}
\value{
An object of class 'khaldoon_test' containing test results and components
}
\description{
A robust test for detecting periodic patterns in time series data using
composite spectral statistics and bootstrap inference.
}
\details{
The bootstrap procedure uses a permutation test (randomization test),
which randomly reorders the data without replacement. This preserves
the empirical distribution while destroying temporal autocorrelation.
This is the appropriate method for testing periodicity and is exact
under the null hypothesis of exchangeability.

The test statistic combines three components:
\itemize{
\item \strong{R (Peak-to-Mean Ratio)}: Measures dominance of strongest peak
\item \strong{S (Significant Peaks)}: Counts significant spectral peaks
\item \strong{C (Energy Concentration)}: Measures energy in significant peaks
}
}
\examples{
# Generate synthetic periodic data
set.seed(123)
t <- 1:100
periodic_data <- sin(2 * pi * t / 20) + rnorm(100, 0, 0.5)

# Perform Khaldoon test
result <- khaldoon_test(periodic_data, n_bootstrap = 100)
print(result)
plot(result)

}
\references{
Good, P. I. (2005). Permutation, Parametric, and Bootstrap Tests of
Hypotheses. 3rd ed. Springer.

Davison, A. C., & Hinkley, D. V. (1997). Bootstrap Methods and their
Application. Cambridge University Press.
}
